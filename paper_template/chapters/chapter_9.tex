\section{Summary of Findings}
In this thesis, a framework was proposed to enhance the trustworthiness of clinical question answering.
The goal is to be as concrete as possible; through context labelling and original-source tracking, supported by a latent action-conditioned model.
The authors demonstrated the compactness of world model (10.1M) along with a calibrated deep ensemble, and the use of Shapley-value attribution.
parameter model. The MAE of the world model is 0.039, and the ECE for "well calibrated" is 0.029.
and generalises (train/test gap < 0.002 MAE). The entire system is enhanced with concept.
coverage and completeness with good RAG baseline (Wilcoxon p =
A new set of 0.046) and calibrated uncertainty and per-document source tracking, previously missing from the baseline.
lacks.



\section{Contributions to the Field}
The contributions are: (i) a trust-oriented reframing of RAG around context labelling and (ii) an explanation of how these contributions can be applied in the context of RAG.
A compact world-model core with supervised reward shaping and (iii) a compact policy core.A compact world-model core with supervised reward shaping, and a compact policy core.
beam-search planning with prospective reasoning in retrieval; and (iv) native calibrated un beam-search planning with prospective reasoning.
involves epistemic/aleatoric decomposition; (iv) Shapley-value source tracking that.
Assigns responsibility to individual documents; and (v) a fair and traceable documentation of
scope, all code, configuration and figure scripts published

\section{Recommendations for Future Work}
Limitations are concrete next steps. The replacement of semi-synthetic dynamics with logged is an improvement.It is an improvement to replace semi-synthetic dynamics with logged.
True treatment planning would be possible if there were intervention–outcome data (e.g. intensive-care records).
Replacing the stochastic causal prior with a medical ontology that has been developed, such as SNOMED-CT.
would enhance the graph reasoning. Addressing class imbalance with re-weighting or focal
Calibration would not be sacrificed if objectives were set to increase the macro-F1. Lastly, a potential clinician.Last, but not least, a potential clinician.
An in-the-loop study would be a study that investigates whether there is a measurable benefit from using context labels and source tracking.Automation bias in practice.